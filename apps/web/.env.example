VITE_DEFAULT_ENTRY_ID=1965441

# API base URL. Empty = same-origin (dev uses the Vite proxy to :8787).
# In production on Cloudflare Pages set this to the Worker URL, e.g.
# VITE_API_BASE=https://fplq-api.fplq.workers.dev
VITE_API_BASE=
